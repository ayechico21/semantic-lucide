{
  "name": "semantic-lucide",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.25.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r --filter ./packages/** build",
    "dev": "pnpm -r --filter ./packages/** dev",
    "clean": "pnpm -r --filter ./packages/** clean"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.6.0"
  }
}
